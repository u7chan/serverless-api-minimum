{
  "name": "serverless-api-minimum",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "ts-node ./scripts/build.ts",
    "dev": "yarn build --watch & sls offline --reloadHandler",
    "deploy": "yarn build && sls deploy",
    "destroy": "sls remove"
  },
  "dependencies": {
    "express": "^4.18.2",
    "http-status-codes": "^2.2.0",
    "serverless-http": "^3.1.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.15.7",
    "esbuild": "^0.17.12",
    "serverless": "^3.28.1",
    "serverless-offline": "^12.0.4",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.2"
  }
}
